/** Game version label (keep aligned with `package.json` and changelog UI). */
export const APP_VERSION = '0.1.0';

/** Hotbar: seed slots + water + harvest (see `index.html` order). */
export const HOTBAR_SLOT_COUNT = 5;

export const HARVEST_SLOT_INDEX = 4;
